import { Input, Flex, Heading } from "native-base";

export default function Story() {
    return (
        <Flex pt={5} flex={1} bg='primary.100'>
            <Heading color='secondary.100' mb={10}>Story</Heading>
        </Flex>
    )
}